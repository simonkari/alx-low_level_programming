#include <stdio.h>
/**
 * main - A program that prints a line with put fnction
 * return: 0 (Successs)
 * In this program, we use the puts function to print the required message to the console. The puts function automatically adds a new line at the end of the message, so we don't need to add it ourselves.

The main function returns an integer value, which represents the exit status of the program. In this case, we simply return 0 to indicate that the program executed successfully.
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}

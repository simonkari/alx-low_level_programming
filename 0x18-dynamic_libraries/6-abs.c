#include "main.h"
/**
* _abs - returns the absolute value of an integer
* @n: the string you want to convert
* Return: Returns the integer value of n
*/
int _abs(int n)
{
if (n < 0)
return (n * -1);
return (n);
}

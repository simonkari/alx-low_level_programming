#include "main.h"

/**
 * mul - multiplies two integers
 * @x: firs number to multiply
 * @y: second number to multiply with the first
 * Return: the result of multiplication
*/

int mul(int x, int y)
{
return (x * y);
}
